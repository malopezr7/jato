name: starter
description: Minimal sensible defaults for AI-assisted development

providers:
  claude: true
  codex: false
  gemini: false

mcp_servers:
  - id: github
    command: npx
    args: [-y, "@modelcontextprotocol/server-github"]
    env: [GITHUB_TOKEN]

permissions:
  auto_execute: false
